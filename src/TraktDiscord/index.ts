import { WebhookClient } from "discord.js";
import { TraktFormatter } from "../Formatters/TraktFormatter";

export default class TraktDiscord {
  //private readonly _webhook: WebhookClient;
  //private readonly _webhookId: string;
  //private readonly _webhookToken: string;

  constructor(formatter: TraktFormatter) {}
}
